{
	"name": "pi-subagents",
	"version": "0.2.0",
	"type": "module",
	"description": "Subagents — labeled background fan-out workers with live per-agent tool, token, and context activity; parallel scheduling, explicit await or idle auto-wake, strict hub-and-spoke isolation, and pi-safety confirmation.",
	"keywords": [
		"pi-package"
	],
	"pi": {
		"extensions": [
			"./extensions"
		]
	},
	"peerDependencies": {
		"@earendil-works/pi-coding-agent": "*",
		"@earendil-works/pi-tui": "*",
		"typebox": "*"
	}
}
