{
  "name": "pi-run-guard",
  "version": "1.0.0",
  "description": "Confirms before session actions that would interrupt an active agent run.",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
