{
  "name": "pi-bookmark",
  "version": "1.0.1",
  "description": "Bookmark entries with labels for /tree navigation via setLabel (/bookmark, /unbookmark, /bookmarks manager).",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
