{
  "name": "pi-codex-usage",
  "version": "1.0.2",
  "type": "module",
  "description": "Shows available Codex subscription rate-limit windows in the status line and via /codex-usage.",
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
