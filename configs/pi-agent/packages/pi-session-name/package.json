{
  "name": "pi-session-name",
  "version": "1.0.1",
  "description": "Name sessions for the session selector via setSessionName (/session-rename [name | --clear]) plus heuristic auto-naming from the first prompt.",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
